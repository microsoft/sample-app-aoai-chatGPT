import { makeStyles, shorthands, tokens } from "@fluentui/react-components";

export const MicrophoneStyles = makeStyles({
    microphone: {
        color: tokens.colorBrandForeground1,
    },
    micIconRecording: {
        color: tokens.colorPaletteRedForeground1, 
    },
});
import { EventListenerOptions, EventTypes } from './types';
export declare function EventListener<T extends EventTypes>(props: EventListenerOptions<T>): null;
export declare namespace EventListener {
    var defaultProps: {
        capture: boolean;
    };
}
